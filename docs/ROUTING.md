# Model Routing
- Use **GPT-5 Codex** for features, endpoints, forms, tests, PDF mappers, CI.
- Use **Claude Code** for repo-wide refactors, dependency upgrades that break builds, multi-step DB migrations, merge conflicts, performance/a11y sweeps, large diff reviews (≥300 LOC).
